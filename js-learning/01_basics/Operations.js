let x=2
let y=x++       //In this type of increment firest it return value to variable then inctement it
console.log(`x:${x},y:${y}`)

let a=3
let b=++a       //In this type of increment first it increment the value then assign it to the variables
console.log(`a:${a},b:${b}`)